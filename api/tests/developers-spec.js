var should = require("should");


describe("#Developers", function () {
	
	describe("newUser", function () {

		it("should create a new user correctly")
		it("should return error upon invalid data")
	})

	describe("update", function () {
		
		it("should update the user correctly")

	})

	describe("getDetails", function () {
		it("should return a valid json for a valid user")
		it("should return a 404 error when the user id is not valid")
	})
})